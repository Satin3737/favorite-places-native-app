import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    button: {
        padding: 6,
        marginVertical: 2,
        marginHorizontal: 8,
        borderRadius: 24
    },
    pressed: {
        opacity: 0.8
    }
});

export default styles;
